<%= form_for @myplant, html: { multpart: true } do |f| %>
  <% if @myplant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myplant.errors.count, "error") %> prohibited this plant from being saved:</h2>

      <ul>
      <% @myplant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= f.label :image %>
    <%= f.file_field :image, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-success btn-lg" %>
  </div>
<% end %>